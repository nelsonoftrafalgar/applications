import { PopoverTrigger } from '@radix-ui/react-popover'
import styled from 'styled-components'

export const PopoverTriggerStyled = styled(PopoverTrigger)`
	background-color: ${({ theme }) => theme.colors.backgrounds.white};
`
